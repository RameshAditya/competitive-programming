#include<bits/stdc++.h>
using namespace std;
int main(){
	int n;
	scanf("%d",&n);
	while(n!=0){
		stack<int> s;
		int hts[n];
		for(int i=0;i<n;i++)scanf("%d",&hts[i]);
		int i=0;
		int curarea=0;
		int maxarea=0;
		while(i<n){
			if(s.empty()||h[i]>h[s.top()])s.push(i++);
			else{
				int tp=s.top();
				
			}
		}
		
		scanf("%d",&n);
	}
}
